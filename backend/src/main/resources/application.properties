spring.application.name=backend
server.port=8091


spring.datasource.url=jdbc:postgresql://localhost:5432/MediStock
spring.datasource.username=postgres
spring.datasource.password=Password
spring.jpa.hibernate.ddl-auto=update

# ─── Gmail SMTP Configuration for Email Alerts ─────────────────
# 
# RECOMMENDED: Use environment variables for security (avoid hardcoding secrets).
# Set these in your system environment or IDE run configuration:
#   SPRING_MAIL_USERNAME=your-email@gmail.com
#   SPRING_MAIL_PASSWORD=your-app-password
#   APP_EMAIL_FROM_ADDRESS=your-email@gmail.com
#
# To generate a Gmail App Password:
#   1. Go to https://myaccount.google.com/security → "2-Step Verification" → Turn ON
#   2. Go to https://myaccount.google.com/apppasswords → Generate "Mail" app password
#   3. Copy the 16-character password and use it below
#
spring.mail.host=smtp.gmail.com
spring.mail.port=587
# Use ${SPRING_MAIL_USERNAME} or hardcode for testing:
spring.mail.username=aryanvatsa05@gmail.com
spring.mail.password=hqgq yifz lcge crem
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Email sender address (must match spring.mail.username)
app.email.from-address=aryanvatsa05@gmail.com
app.email.from-name=MediStock Pharmacy System
# Comma-separated list of user roles that receive email alerts
# Available roles: Admin, Pharmacist, Staff, Supplier
app.email.alert-recipient-roles=Admin,Pharmacist

