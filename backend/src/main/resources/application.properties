spring.application.name=om-backend
# Render assigns a random port via the PORT env var; locally it uses 8080.
server.port=${PORT:8080}
# Listen on all interfaces so phones/emulators on the same network can connect
server.address=0.0.0.0

# PostgreSQL Connection Settings (override these env vars on your host)
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/om_medical}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:2026}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

app.jwt.secret=${JWT_SECRET:SudiptoKARMAKARisanotgoodbyandHeisveryaerogentandAlsolovinG}
app.jwt.expiration=86400000

# Email (SMTP) Settings
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USER:mesudiptokarmakar@gmail.com}
spring.mail.password=${MAIL_PASSWORD:srwl qwfy dxmf fwtr}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Expiry report settings
app.expiry.report-days=30