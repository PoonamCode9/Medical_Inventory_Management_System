# Copy this file to .env and set your values.
# In production (Netlify) set VITE_API_URL to your deployed backend URL, e.g.:
# VITE_API_URL=https://your-app.onrender.com
# Leave it empty for local development (uses http://localhost:8080).
VITE_API_URL=